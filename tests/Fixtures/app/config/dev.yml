parameters:
  muse.loaded_config: %kernel.environment%

framework:
  secret: notsosecret
  templating:
    engines: ['twig']

twig:
  debug: "%kernel.debug%"
  strict_variables: "%kernel.debug%"
  paths:
    "%kernel.root_dir%/src/Resources/views": Fixtures
